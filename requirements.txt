# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Config
pydantic-settings>=2.3.0

# Database
sqlalchemy>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.1
pgvector>=0.3.0

# Cache / broker
redis[asyncio]>=5.0.4
celery[redis]>=5.4.0

# Data
yfinance>=0.2.40
pandas>=2.2.0
numpy>=1.26.0
ta>=0.11.0

# News / filings
newsapi-python>=0.2.7
feedparser>=6.0.11
httpx>=0.27.0
beautifulsoup4>=4.12.3
lxml>=5.2.2
pdfminer.six>=20221105

# AI / ML
groq>=0.9.0
sentence-transformers>=3.0.0
langgraph>=0.2.0

# Broker / trading
# Default: paper_broker (no extra deps — uses yfinance + postgres)
# BROKER=upstox: no extra deps (uses httpx, already installed)
# BROKER=angel:  pip install smartapi-python>=1.3.0 pyotp>=2.9.0  (requires Demat account)

# Observability
structlog>=24.2.0
tenacity>=8.3.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.7
httpx>=0.27.0
